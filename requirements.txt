bcrypt==4.1.3

aws-cdk-lib==2.180.0
aws-cdk.aws-lambda-python-alpha==2.180.0a0
constructs>=10.0.0,<11.0.0
boto3==1.34.162
python-dotenv==1.0.1
opensearch-py==2.6.0
requests-aws4auth==1.3.1
PyPDF2==3.0.1
pycryptodome==3.20.0

# CheXOne Production – Python Dependencies
# Install: pip install -r requirements.txt

# ── Core ML ──────────────────────────────────────────────────────
torch>=2.5.0
torchvision>=0.20.0
transformers>=5.0.0
accelerate>=1.0.0
qwen-vl-utils>=0.0.14

# ── Image / DICOM ───────────────────────────────────────────────
Pillow>=10.0.0
pydicom>=3.0.0
numpy>=1.24.0

# ── Config / IO ──────────────────────────────────────────────────
PyYAML>=6.0

# ── AWS (only needed for SageMaker deployment, not local runs) ──
sagemaker>=2.200.0